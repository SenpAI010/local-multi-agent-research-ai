requests>=2.31
pillow>=10.0
pypdf>=4.0
psutil>=5.9
chromadb>=0.5
numpy>=1.26
matplotlib>=3.8
mpmath>=1.3

# Optional but used by the screen/audio observer features.
# If installation is heavy on your machine, install these only when needed:
easyocr>=1.7
pytesseract>=0.3
sounddevice>=0.4
faster-whisper>=1.0
